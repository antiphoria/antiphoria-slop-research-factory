# README badge: Python unit tests.
name: CI Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    uses: antiphoria/.github/.github/workflows/qg-python-test.yml@main
    with:
      python-version: "3.12"
