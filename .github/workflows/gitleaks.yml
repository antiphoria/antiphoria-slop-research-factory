# README badge: Gitleaks secret scan.
name: Gitleaks

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    uses: antiphoria/.github/.github/workflows/qg-gitleaks.yml@main
    secrets: inherit
