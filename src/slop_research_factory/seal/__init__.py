"""Provenance seal integration (slop-seal / slop-cli).

The full ``seal_step`` implementation may be supplied from a companion
package or wired in as the product matures.  Until then,
``slop_research_factory.seal.helpers`` exposes a stub for tests
and a clear :class:`NotImplementedError` at runtime if invoked
unpatched.
"""
