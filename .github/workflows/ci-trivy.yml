# README badge: Trivy filesystem scan.
name: CI Trivy

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trivy:
    uses: antiphoria/.github/.github/workflows/qg-trivy.yml@main
    with:
      policy-ref: main
